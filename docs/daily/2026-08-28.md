#задачи 28.08.2026

Этап 0 целиком — задачи 1–9 из [tasks.md](../tasks.md).

1. Вычистить `attached_assets/` и `ion-ev-source.zip` из истории git, force-push
2. `.gitignore`: `attached_assets/`, `*.zip`, `.env`, `.env.*`
3. `admin.ts` — срок жизни токена 12 часов
4. `admin.ts` — убрать fallback-секрет
5. Зашифровать `operators.api_credentials`
6. `GET /api/payment-methods` — фильтр по `user_id`
7. Заполнить `replit.md`
8. GitHub Actions: typecheck на push
9. Rate limit на публичные эндпоинты

---

#итог 28.08.2026

1. Вычистить историю git ✅ — репозиторий 12.06 → **2.02 МБ**, 73 коммита.
   Ключей Google, Яндекса и API Ninjas в истории больше нет. Бэкап полной
   старой истории: `../EVGO-backup-before-rewrite.bundle`
2. `.gitignore` ✅ — плюс `.claude/` (симлинки с абсолютными путями машины)
3. Срок жизни админ-токена ✅ — 12 часов, проверка в `verifyToken`
4. Fallback-секрет убран ✅ — **сервер не стартует без `ADMIN_JWT_SECRET`**
5. Шифрование `api_credentials` ✅ — AES-256-GCM, `src/lib/secrets.ts`
6. Фильтр по `user_id` ✅ — в `GET` и `DELETE /api/payment-methods`
7. `replit.md` ✅ — команды, переменные, карта репозитория, решения, грабли
8. CI ✅ — две джобы: бэкенд с админкой (блокирующая) и мобилка (пока нет)
9. Rate limit ✅ — 300/мин на API, 30/мин на геокодер, 10 попыток входа за 15 мин

**Сверх списка** — вскрылось по ходу:

10. `POST`, `PUT`, `DELETE /api/operators` были **полностью без авторизации** —
    закрыты `adminAuth` ✅
11. `sessions.ts` не импортировал `connectorsTable` и `connectorWatchersTable` —
    старт и остановка зарядки на коннекторе падали в рантайме ✅
12. `import-ocm.ts` тянул `pg` мимо зависимостей пакета — переведён на общий
    пул из `@workspace/db` ✅
13. `stations.ts`: `distance_km` выводился как `null` и не принимал число ✅
14. `pnpm-workspace.yaml`: `allowBuilds: esbuild` — без него любой `pnpm run`
    падал с `ERR_PNPM_IGNORED_BUILDS` ✅
15. Заведена задача 97: 15 накопленных ошибок типов в мобильном пакете ПАУЗА

**Проверено:** `pnpm run typecheck` — библиотеки, api-server, админка,
mockup-sandbox и scripts проходят чисто. Мобильный пакет — 15 ошибок,
все до сегодняшних правок, вынесены в задачу 97.

---

## Второй заход: этап 1, задачи 11–19

16. `users`: `phone` (unique), `phone_verified_at`, `language` (uz/ru/en),
    `email` больше не обязателен ✅ — задача 11
17. Таблица `otp_codes` ✅ — задача 12. Хранится HMAC кода, не сам код
18. Ограничения на запрос кода ✅ — задача 13. По номеру считаются в БД
    (раз в минуту, 5 в час) — счётчик в памяти обнулялся бы при перезапуске.
    Поверх — предел по IP в `app.ts`
19. Таблица `refresh_tokens` ✅ — задача 14. Хранится SHA-256, есть отзыв
20. `POST /api/auth/request-code` ✅ — задача 15
21. `POST /api/auth/verify-code` ✅ — задача 16. Заводит пользователя, если
    номер новый, отдаёт пару токенов
22. `POST /api/auth/refresh` ✅ — задача 17. С ротацией: старый refresh
    гасится, украденный живёт только до ближайшего обновления
23. `POST /api/auth/logout` ✅ — задача 18
24. Middleware `requireAuth` и `optionalAuth` ✅ — задача 19
25. Сверх списка: `GET /api/auth/me`, отправка SMS через Eskiz с заглушкой
    на время, пока нет договора (`lib/sms.ts`)

**Проверено:** `pnpm run check` зелёный; 23 проверки логики авторизации
(нормализация номера, хеширование кодов, подпись и протухание токенов,
подделка подписи и payload, ротация refresh) — все прошли.

**Задача 20 намеренно не сделана.** Замена `user_id` на `req.userId` во всех
роутах сломает работающее приложение: в нём ещё нет экрана входа и токен
неоткуда взять. Её нужно выкатывать одним куском с задачами 21–26.

---

## Третий заход: этап 1, задачи 21–27

26. Экран ввода номера ✅ — задача 21. Маска `+998 90 123 45 67`, проверка
    кода оператора до отправки
27. Экран ввода кода ✅ — задача 22. Шесть ячеек поверх одного скрытого поля —
    иначе автоподстановка кода из SMS не работает. Отправка сама по шестой
    цифре, отсчёт до повторной отправки
28. Экран профиля ✅ — задача 23. Имя и язык, можно пропустить
29. Токены в `expo-secure-store` ✅ — задача 24. На вебе `localStorage`:
    SecureStore там не работает вообще
30. `DEMO_USER_ID` убран ✅ — задача 25. `AppContext` берёт пользователя из
    `AuthContext`, интерфейс для экранов не изменился
31. Обработка 401 ✅ — задача 26. `customFetch` при 401 один раз молча
    обновляет пару токенов и повторяет запрос; параллельные запросы ждут
    одного обновления, а не жгут по refresh-токену каждый
32. Выход и удаление аккаунта ✅ — задача 27. Кнопки в настройках, удаление
    в два касания. `DELETE /api/auth/me` обезличивает профиль, но оставляет
    сессии зарядок — они нужны операторам для сверки расчётов
33. Сверх списка: `PATCH /api/auth/me`, защита маршрутов в `app/_layout.tsx`,
    эндпоинты `/auth/*` описаны в `openapi.yaml`

**Найдено и исправлено по ходу:** удаление аккаунта и сохранение профиля
уходили без заголовка `Authorization` — сервер вернул бы 401. Оба запроса
перенесены в `AuthContext`, где есть токен.

**Проверено:** `pnpm run check` зелёный; в мобильном пакете по-прежнему ровно
15 ошибок — все в файлах, которых правки не касались (задача 97).

**Вход сделан обязательным.** Гостевой просмотр карты убран: с ним половина
экранов получала бы пустой `userId`. Заведена задача 98 — вернуть гостевой
режим отдельно, аккуратно.

---

## Что нужно сделать руками

**1. Обновить рабочую копию на Replit.** История переписана, старая ветка
разошлась с новой. В Shell:

```bash
git fetch origin
git reset --hard origin/main
```

Если на Replit остались незакоммиченные правки — сначала сохранить их
(`git stash` или копией файлов), они не переживут `reset --hard`.

**2. Проверить, что задан `ADMIN_JWT_SECRET`.** Это ломающее изменение: без
переменной сервер теперь **не запускается**. Сгенерировать:

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

**3. По желанию — `CREDENTIALS_ENCRYPTION_KEY`** (той же командой). Без неё
всё работает, но сохранить учётные данные оператора в админке не получится:
вернётся 503 с понятным текстом.

---

**4. Применить схему БД.** Появились три изменения в `users` и две новые
таблицы:

```bash
pnpm --filter @workspace/db run push
```

Если команда зависнет на вопросе про уникальный индекс (drizzle-kit требует
TTY) — примените готовый SQL:

```bash
psql "$DATABASE_URL" -f docs/sql/2026-08-28-auth.sql
```

**5. Проверить вход.** Пока шлюз не настроен, код уходит в лог сервера:

```bash
curl -X POST localhost:$PORT/api/auth/request-code   -H 'Content-Type: application/json' -d '{"phone":"+998901234567"}'
# код появится в логе строкой [SMS STUB]
curl -X POST localhost:$PORT/api/auth/verify-code   -H 'Content-Type: application/json' -d '{"phone":"+998901234567","code":"123456"}'
```

---

## Дальше по списку

Этап 1 закрыт, кроме задачи 20 — перевода всех 17 групп роутов на
`req.userId`. Теперь она безопасна: приложение умеет входить и присылает
токен. Это следующий шаг, и его стоит делать отдельным коммитом — он трогает
каждый обработчик, и откатывать смешанные правки было бы больно.

Задача 10 (договор с Eskiz) по-прежнему не наша: без имени отправителя SMS
уходить не будут, но весь код к этому готов — достаточно добавить
`ESKIZ_EMAIL` и `ESKIZ_PASSWORD` в Secrets, заглушка отключится сама.
